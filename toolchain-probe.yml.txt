name: ASUSTOR Toolchain Probe

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Probe ASUSTOR toolchain
        run: echo probe
